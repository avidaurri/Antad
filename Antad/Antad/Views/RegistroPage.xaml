<?xml version="1.0" encoding="utf-8" ?>
<CarouselPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Antad.Views.RegistroPage" 
              Title="REGISTRO" >

    <ContentPage
        xmlns:i18n="clr-namespace:Antad.Helpers"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
           
             BackgroundImageSource="fondosplash">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackLayout Grid.ColumnSpan="3" BackgroundColor="Black" Orientation="Horizontal">
                <Label 
                    Text="Inicia tu registro escribiendo los datos de acceso a la aplicación"
                    HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
            </StackLayout>

           <StackLayout Orientation="Vertical" 
                         BackgroundColor="Purple"
                         Grid.Row="1" Grid.Column="0"
                        VerticalOptions="Center">
                <Image Source="logoantad" VerticalOptions="Center" WidthRequest="30" />
            </StackLayout>
            <StackLayout Orientation="Vertical" 
                         BackgroundColor="Green"
                         Grid.Row="1" Grid.Column="1">
                <Label Text="cuestionario " />
            </StackLayout>
            <StackLayout Orientation="Vertical" Grid.Row="1" Grid.Column="2"
                         BackgroundColor="Brown" VerticalOptions="Center">
                <Image Source="logoantad" VerticalOptions="Center" WidthRequest="30"  />
            </StackLayout>

            <StackLayout Grid.ColumnSpan="3"
                       Orientation="Horizontal" HorizontalOptions="Center" Grid.Row="2">
                <Image Source="indicador_morado" WidthRequest="50"/>
                <Image Source="indicador_gris" WidthRequest="35"/>
                <Image Source="indicador_gris" WidthRequest="35"/>
                <Image Source="indicador_gris" WidthRequest="35"/>
                <Image Source="indicador_gris" WidthRequest="35"/>
                <Image Source="indicador_gris" WidthRequest="35"/>
            </StackLayout>
            <StackLayout Grid.ColumnSpan="3" 
                         Orientation="Horizontal" HorizontalOptions="Center" Grid.Row="3">
                <Label 
                    HorizontalOptions="Center"
                    Text="1"
                    FontSize="50"
                    TextColor="Navy"
                    FontFamily="{StaticResource MontserratBold}"
                    HorizontalTextAlignment="Center"/>
                <Label 
                    HorizontalOptions="Center"
                    Text="DE"
                    FontSize="30"
                    TextColor="DimGray"
                    FontFamily="{StaticResource MontserratBold}"
                    HorizontalTextAlignment="Center"/>
                <Label 
                    HorizontalOptions="Center"
                    Text="6"
                    FontSize="50"
                    TextColor="Navy"
                    FontFamily="{StaticResource MontserratBold}"
                    HorizontalTextAlignment="Center"/>
            </StackLayout>

        </Grid>


        
    </ContentPage>
    
    <!--<ContentPage
        xmlns:i18n="clr-namespace:Antad.Helpers"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
           
             BackgroundImageSource="fondosplash">
        <ContentPage.Content>
        <ScrollView
            BindingContext="{Binding Register}">
            <StackLayout
                Padding="5">
                <Image
                    Source="{Binding ImageSourceFoto}">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CargarImagenCommand}"/>
                    </Image.GestureRecognizers>
                </Image>
                <Label
                    FontSize="Small"
                    HorizontalOptions="Center"
                    Text="Presiona para cargar tu foto de perfil">
                </Label>
                <Frame BorderColor="MediumPurple"  BackgroundColor="Transparent"  HasShadow="True" CornerRadius="10" Margin="5" Padding="20,0,20,0" VerticalOptions="Start">
                    
              
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                     </Grid.RowDefinitions>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="0"
                        Text="{Binding Nombre}"
                        Placeholder="Nombre">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="1"
                        Text="{Binding ApellidoPaterno}"
                        Placeholder="Apellido Paterno">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="2"
                        Text="{Binding ApellidoMaterno}"
                        Placeholder="Apellido Materno">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="3"
                        Keyboard="Email"
                        Text="{Binding Email}"
                        Placeholder="Email">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="4"
                          Text="{Binding Login}"
                        Placeholder="Login">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="5"
                        IsPassword="True"
                        Text="{Binding Password}"
                        Placeholder="Password">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="6"
                        Text="{Binding Curp}"
                        Placeholder="CURP">
                    </Entry>

                    <Entry
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.ColumnSpan="2"
                        Grid.Row="7"
                        Text="{Binding EmpresaInteres}"
                        Placeholder="Empresa Interes">
                    </Entry>
                    <Label
                        Grid.Column="0"
                          FontFamily="{StaticResource MontserratRegular}"
                        Grid.Row="8"
                        Text="Puesto solicitado"
                        VerticalOptions="Center">
                    </Label>
                    <Picker x:Name="IndustryTypePicker1" 
                              FontFamily="{StaticResource MontserratRegular}"
                        Grid.Column="1"
                        Grid.Row="8"
                            SelectedIndex="{Binding Puesto}">
                        <Picker.Items>
                            <x:String>Promotor</x:String>
                            <x:String>Supervisor</x:String>
                            <x:String>Intramuro</x:String>
                        </Picker.Items>
                    </Picker>


                </Grid>
                </Frame>



            </StackLayout>
        </ScrollView>
    </ContentPage.Content>    
    </ContentPage>


    <ContentPage
        xmlns:i18n="clr-namespace:Antad.Helpers"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Title="REGISTRO"
             BackgroundImageSource="fondosplash">
        <ContentPage.Content>
            <ScrollView
            BindingContext="{Binding Register}">
                <StackLayout
                Padding="5">
                    <Image
                    Source="{Binding ImageSourceFoto}">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding CargarImagenCommand}"/>
                        </Image.GestureRecognizers>
                    </Image>
                    <Label
                    FontSize="Small"
                    HorizontalOptions="Center"
                    Text="Presiona para cargar tu foto de perfil">
                    </Label>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackLayout Grid.Column="0" Orientation="Horizontal" HorizontalOptions="Center">
                            <StackLayout HorizontalOptions="Center">
                                <Image WidthRequest="70" HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                    Source="{Binding ImageSourceIdentificacion}">
                                   
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding CargarIdentificacionCommand}"/>
                                    </Image.GestureRecognizers>
                                </Image>
                                <Label
                                FontSize="Micro"
                                HorizontalOptions="Center"
                                Text="Presiona para cargar tu identificación">
                                </Label>
                            </StackLayout>

                        </StackLayout>
                        <StackLayout Grid.Column="1" Orientation="Horizontal" HorizontalOptions="Center">
                            <StackLayout HorizontalOptions="Center">
                                <Image WidthRequest="70" HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                   x:Name="cargarAni"
                                    Source="{Binding ImageSourceComprobante}">
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding CargarComprobanteCommand}"/>
                                    </Image.GestureRecognizers>
                                </Image>
                                <Label
                                FontSize="Micro"
                                HorizontalOptions="Center"
                                Text="Presiona para cargar tu comprobante domiciliario">
                                </Label>
                            </StackLayout>
                        </StackLayout>
                    </Grid>

                    <ActivityIndicator
                    IsRunning="{Binding IsRunning}"
                    VerticalOptions="CenterAndExpand">
                    </ActivityIndicator>

                    <Button
                    BackgroundColor="DeepSkyBlue"
                    Command="{Binding SaveCommand}"
                    HeightRequest="46"
                    IsEnabled="{Binding IsEnabled}"
                    Text="Guardar"
                    TextColor="White">
                    </Button>
                </StackLayout>
            </ScrollView>
        </ContentPage.Content>
    </ContentPage> -->

</CarouselPage>